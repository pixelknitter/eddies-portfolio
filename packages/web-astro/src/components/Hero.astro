---
import { Image } from "astro:assets";

interface Props {
    src: string;
    alt: string;
}

const { src, alt } = Astro.props;

---

<div class="hero-gif">
  <Image width="300" height="259" alt={alt} src={src}/>
</div>