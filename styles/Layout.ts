export const Layout = {
  Container: 10,
  Paragraph: 14,
  Corners: 12,
  // Adhere to the rule of 3s - TODO: make this more clear and extend from the used value
  multiplier: (scalar: number) => scalar * 3,
}
